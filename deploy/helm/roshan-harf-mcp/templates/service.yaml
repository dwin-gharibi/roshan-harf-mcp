apiVersion: v1
kind: Service
metadata:
  name: {{ include "roshan-harf-mcp.fullname" . }}
  labels:
    {{- include "roshan-harf-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "roshan-harf-mcp.selectorLabels" . | nindent 4 }}
